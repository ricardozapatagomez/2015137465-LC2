@model IEnumerable<_2015104916.Entities.Entities.Volante>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NumeroSerie)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NumeroSerie)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodigoVolante }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodigoVolante }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodigoVolante })
        </td>
    </tr>
}

</table>
